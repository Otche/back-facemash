import { Test, TestingModule } from '@nestjs/testing';
import { FacemashController } from './facemash.controller';
import { FacemashService } from './facemash.service';

describe('AppController', () => {
  /*let appController: FacemashController;

  beforeEach(async () => {
    const app: TestingModule = await Test.createTestingModule({
      controllers: [FacemashController],
      providers: [FacemashService],
    }).compile();

    appController = app.get<FacemashController>(FacemashController);
  });

  describe('root', () => {
    it('should return "Test !!!"', () => {
      expect(appController.getTest()).toBe('Test !!!');
    });
  });*/
});
